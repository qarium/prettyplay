prompt: |
  Answer (feedbacks, proposes, questions and etc) in Russian language.

stages:
  define:
    skip: true
  discover:
    skip: true
  propose:
    agent: manager
  task-review:
    agent: reviewer